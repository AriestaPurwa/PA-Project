plan tombol save permanently
@if($project->is_guest)

    <a href="/login" class="btn app-btn">
        Login to Save Permanently
    </a>

@endif


plan clean up temp db
Project::where('is_guest', true)
    ->where('created_at', '<', now()->subDay())
    ->delete();

    gpt step 4